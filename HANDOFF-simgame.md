# 이식용 핸드오프 ② — 시뮬 제조기(연애 미니게임)

> 새 세션에 이 내용을 붙여넣고 시작. 이전 대화 없이도 이어서 설계·구현할 수 있게 정리했습니다.
> 자매 문서: `HANDOFF.md`(ani-cliché 전반), `HANDOFF-improve.md`(개선 작업). 겹치는 인프라는 그 문서 참고.

## 0. 필수 규칙
- **한국어 대화. 전문적 톤 — 욕설 금지**(사용자가 화나서 욕해도 따라하거나 되받지 말 것).
- **"물어가면서 해야지"** — 큰 기능은 설계안 먼저 제안·확인 후 구현. 이 게임은 아직 **설계 승인 대기** 단계.
- 검증 전엔 "가설"이라 말할 것(검증 없이 "원인 찾았다" 확정 금지). 기존 기능 임의 제거 금지.
- **절차(매 변경):** 구현 → `npx tsc --noEmit`(워커 만졌으면 `node --check worker/*.mjs`) → 커밋 → `git push origin main` → SHA 보고. 로컬 `npm run build`는 Google Fonts 네트워크로 실패하니 tsc로 게이트.

## 1. 배경 — 무엇을 만드나
- 프로젝트: **aninews-maker** (`C:\myapps\aninews-maker21`). Next.js(App Router)/Vercel + Render Worker + Redis + Vercel Blob. 텍스트=Anthropic Claude, TTS=ElevenLabs/Typecast, 영상=Grok(주력)/fal/Kling.
- 이미 만든 **ani-cliché**(연애 클리셰 영상 생성) 위에 얹는 **미니 연애 게임**. 사용자 비전: "Claude API를 연애 상대 페르소나로 대화 + 예약된 심쿵 클리셰 영상 컷씬 + 양산." 투자 유치도 염두.

## 2. 게임 코어 (사용자 확정)
> 여러 상대와 대화하며 **친밀도(0~100)**를 올려, **고백을 받아내거나** 플레이어가 먼저 **고백해 수락**받으면 승리.

## 3. 제안한 설계 (사용자 결정 대기 — 2026-07-15 시점)
- **상대 = `castMembers` 재사용.** ani-cliché에서 만든 인물(이름·아키타입·포트레이트·목소리)을 그대로 상대 캐릭터로. 아키타입 → Claude 페르소나 시스템 프롬프트로 변환.
- **턴 판정:** 매 대화 턴마다 Claude가 `{ reply, affinityDelta }` JSON 반환(친밀도 증감 포함). 상태는 Redis `simgame/`(정의)·`simplay/`(플레이 세션)에 저장. 엔드포인트 `POST /api/sim/chat`.
- **마일스톤 컷씬:** 친밀도 25/50/75 도달 시 미리 만들어둔 **클리셰 영상**을 컷씬으로 재생(양산해둔 심쿵 장면 연결).
- **엔딩:** 고백 성공 / 거절 분기.

## 4. 대기 중 결정 4가지 (사용자에게 물어서 확정할 것)
1. **위치:** 어디에 넣나 — (추천) aninews 상단 탭에 "🎮 시뮬 제조기" 추가. 
2. **대화 모델:** (추천) **Haiku** — 턴이 많고 저렴·빠름. 감정 깊이 필요하면 Sonnet 옵션.
3. **컷씬 소스:** (추천) **기존 ani-cliché 프로젝트 연결** — 게임 정의에서 마일스톤별 프로젝트 id 지정. (신규 자동생성은 후순위.)
4. **대화 TTS:** (추천) **텍스트만** 우선(비용·지연). 목소리는 후속 옵션(castVoices 재사용 가능).

## 4.5 비용 결론 (사용자에게 이미 보고함 — 재계산 불필요)
- 대화 1턴 = 입력(페르소나 ~1천 토큰+이력) / 출력 ~150토큰. Haiku 4.5 $1/$5, Sonnet $3/$15 per 1M.
- **한 상대 30턴 풀 플레이: Haiku ≈ 160원(캐싱 시 ~55원), Sonnet ≈ 480원(캐싱 시 ~145원).** 1판(상대 3명) ≈ 150~500원. 컷씬 영상은 기존 완성본 재사용이라 플레이당 추가 비용은 대화뿐 — 클리셰 영상 1편 제작비로 수십 판 플레이 가능.
- 구현에 넣을 비용 레버: ①프롬프트 캐싱(시스템+이력 프리픽스 — Haiku는 4,096토큰부터 캐시됨) ②이력 슬라이딩 윈도우(최근 ~20턴+요약) ③max_tokens ~300 ④recordCost 재사용(비용 대시보드 적산).
- 결론: Haiku+캐싱이면 공유 배포해도 부담 없음. 고백·거절 순간만 Sonnet 혼합도 저렴.

## 5. 붙일 수 있는 기존 자산
- `Project.castMembers`(CastMember[]) — 인물 원천. `cast`/`castVoices`는 파생 미러.
- 아키타입 칩·포트레이트(웹툰 변환, 딥페이크 잠금)·목소리 프리뷰(`/api/tts/preview`) 이미 존재.
- Claude 호출: `lib/anthropic.ts`(`getAnthropic`, `MODELS.haiku`/`.sonnet`). Redis·Blob 헬퍼: `lib/projectStore.ts`, `lib/blob.ts`.
- 클리셰 영상 컷씬은 완성본 `finalVideoUrl`/`cleanVideoUrl`(영상만 합성) 사용.

## 6. 첫 지시 예시
"HANDOFF-simgame.md 읽었지. 시뮬 제조기, 물어가면서 가자 — 먼저 4가지 결정(위치·모델·컷씬·TTS) 확인받고, 데이터 모델(simgame/simplay)과 /api/sim/chat 판정 흐름 설계안부터 제안해줘. 상대는 castMembers 재사용."
